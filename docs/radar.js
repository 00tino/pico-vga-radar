const EMBED=new URLSearchParams(location.search).has('embed');
if(EMBED) document.body.classList.add('embed');
const AIRPORTS=[['AEP','Aeroparque','Buenos Aires',-34.559,-58.416],['EZE','Ezeiza','Buenos Aires',-34.822,-58.536],['COR','Cordoba','Cordoba',-31.310,-64.208],['MDZ','Mendoza','Mendoza',-32.832,-68.793],['BRC','Bariloche','Bariloche',-41.151,-71.158],['JFK','JFK','Nueva York',40.641,-73.778],['LGA','LaGuardia','Nueva York',40.777,-73.872],['LAX','Los Angeles','Los Angeles',33.942,-118.408],['MIA','Miami','Miami',25.796,-80.287],['ATL','Atlanta','Atlanta',33.641,-84.428],['DXB','Dubai','Dubai',25.253,55.365],['LHR','Heathrow','Londres',51.470,-0.454],['MAD','Madrid','Madrid',40.472,-3.563],['GRU','Sao Paulo','Sao Paulo',-23.436,-46.473],['SCL','Santiago','Santiago',-33.393,-70.786],['SYD','Sydney','Sydney',-33.946,151.177],['NRT','Narita','Tokio',35.765,140.386],['HND','Haneda','Tokio',35.549,139.780],['KIX','Kansai','Osaka',34.427,135.244],['ITM','Itami','Osaka',34.785,135.438],['SIN','Changi','Singapur',1.364,103.991],['HKG','Hong Kong','Hong Kong',22.308,113.915],['ICN','Seoul','Seoul',37.460,126.441],['BKK','Bangkok','Bangkok',13.690,100.750],['DOH','Doha','Doha',25.273,51.608]];
const ALINES=[['ARG','Aerolineas Argentinas'],['ANS','Andes'],['JES','JetSMART'],['FBZ','Flybondi'],['LAN','LATAM'],['UAL','United'],['DAL','Delta'],['AAL','American'],['QFA','Qantas'],['SIA','Singapore Airlines'],['ANA','ANA'],['JAL','Japan Airlines'],['UAE','Emirates'],['IBE','Iberia'],['BAW','British Airways'],['CPA','Cathay Pacific'],['KAL','Korean Air'],['QTR','Qatar Airways'],['CMP','Copa'],['AVA','Avianca']];
const HUBS={ARG:['AEP','EZE'],ANS:['AEP','BRC'],JES:['AEP','SCL'],UAL:['ORD','EWR'],DAL:['ATL','JFK'],QFA:['SYD','MEL'],SIA:['SIN'],ANA:['HND','NRT','KIX'],JAL:['HND','NRT'],UAE:['DXB'],IBE:['MAD']};
const AIR={ARG:['AR','Aerolineas Argentinas','#74acdf'],ANS:['OY','Andes','#1c4f9c'],JES:['JA','JetSMART','#ff6600'],LAN:['LA','LATAM','#e0021a'],UAL:['UA','United','#0033a0'],DAL:['DL','Delta','#c8102e'],AAL:['AA','American','#c30021'],QFA:['QF','Qantas','#e0001b'],SIA:['SQ','Singapore Airlines','#f99f1c'],ANA:['NH','ANA','#0032a0'],JAL:['JL','Japan Airlines','#e60012'],UAE:['EK','Emirates','#d71a21'],IBE:['IB','Iberia','#d4092d']};
const ALIAS={};ALINES.forEach(a=>{ALIAS[a[0]]=a[0];ALIAS[a[1].toUpperCase().replace(/[^A-Z]/g,'')]=a[0]});ALIAS.SINGAPORE='SIA';ALIAS.SINGAPOREAIRLINES='SIA';ALIAS.ANDES='ANS';
const ROUTECACHE={};
function letters(s){return (s||'').toUpperCase().replace(/[^A-Z]/g,'')}
function airlineOf(cs){const L=letters(cs);return AIR[L.slice(0,3)]||['',L.slice(0,3)||'ACFT','#334155']}
function kindOf(a){const t=(a.t||'').toUpperCase();const cs=(a.flight||'').toUpperCase();const L=letters(cs);if(/^R44|B06|H125/.test(t))return 'heli';if(cs.startsWith('LV')&&!AIR[L.slice(0,3)])return 'ga';if(AIR[L.slice(0,3)]||/^(A3|A2|B7|E1)/.test(t))return 'air';return 'ga'}
function aliasCode(q){const s=(q||'').toUpperCase().replace(/[^A-Z0-9]/g,'');return ALIAS[s]||ALIAS[s.slice(0,3)]||s.slice(0,3)}
function matchAirline(a,q){if(!q)return true;const code=aliasCode(q);const L=letters(a.flight);const [,name]=airlineOf(a.flight);const raw=q.toUpperCase().replace(/\s+/g,'');return L.startsWith(code)||(name&&name.toUpperCase().replace(/\s+/g,'').includes(raw))}
function fmtAlt(alt){if(alt==='ground'||alt==null)return 'GND';const n=Number(alt);if(!n)return '-';return n>=1000?(n/1000).toFixed(1)+'k ft':Math.round(n)+' ft'}
function ktToMph(gs){return gs==null?'-':Math.round(gs*1.15078)+' mph'}
function hhmm(ts){const d=new Date(ts);return String(d.getHours()).padStart(2,'0')+':'+String(d.getMinutes()).padStart(2,'0')}
function currentCode(){return findAirport(document.getElementById('preset').value||'AEP')[0]}
function findAirport(q){const s=(q||'').toUpperCase().trim();if(s==='CASA'&&home)return ['CASA','Casa','Casa',home[0],home[1]];return AIRPORTS.find(a=>a[0]===s.slice(0,3))||AIRPORTS.find(a=>(a[2]+' '+a[1]+' '+a[0]).toUpperCase().includes(s))||AIRPORTS[0]}
function bindSuggest(input,listEl,rows,fmt,onPick){function show(q){const s=(q||'').toLowerCase();const hits=rows.filter(r=>fmt(r).toLowerCase().includes(s)).slice(0,8);listEl.innerHTML=hits.map(r=>'<button type="button" data-v="'+r[0]+'">'+fmt(r)+'</button>').join('');listEl.style.display=hits.length?'block':'none'}input.addEventListener('input',()=>show(input.value));input.addEventListener('focus',()=>show(input.value));listEl.addEventListener('mousedown',e=>{const b=e.target.closest('button');if(!b)return;onPick(b.dataset.v,b.textContent);listEl.style.display='none'});document.addEventListener('click',e=>{if(!listEl.contains(e.target)&&e.target!==input)listEl.style.display='none'})}
bindSuggest(document.getElementById('preset'),document.getElementById('sugAp'),AIRPORTS,r=>r[0]+' \u2014 '+r[2]+' ('+r[1]+')',(code,label)=>{document.getElementById('preset').value=label;grabForm();setLoad('Cargando '+code+'...');loadLive()});
bindSuggest(document.getElementById('followAl'),document.getElementById('sugAl'),ALINES,r=>r[1]+' ('+r[0]+')',(code,label)=>{document.getElementById('followAl').value=label;document.getElementById('hubs').textContent=(HUBS[code]?'Bases: '+HUBS[code].join(' \u00b7 '):'');grabForm();setLoad('Cargando '+label+'...');loadLive()});
const radar=document.getElementById('radar'),wall=document.getElementById('wall'),loadEl=document.getElementById('load');
let RAW=[],AC=[],running=false,looping=false,view='hybrid',theme='crt_amber',listStyle='fa';
let center=[-34.559,-58.416],includeGround=false,radiusKm=80,maxN=16,rotateS=7,house=false,home=null,demoOver=false;
let beam=-Math.PI/2,lastSweep=0,lastRot=0,page=0,loading=false,followAl='',followFlt='';
let want={air:true,ga:false,biz:false,heli:false};const POS={};
const fAir=document.getElementById('fAir'),fGa=document.getElementById('fGa'),fBiz=document.getElementById('fBiz'),fHeli=document.getElementById('fHeli');
function setLoad(msg){loadEl.textContent=msg||'CARGANDO';loadEl.classList.add('on');const st=document.getElementById('status');if(st)st.textContent=msg||'Cargando...'}
function hideLoad(){loadEl.classList.remove('on')}
function enrich(a){const now=Date.now();const key=(a.flight||'').replace(/\s+/g,'').toUpperCase();const cached=ROUTECACHE[key];if(cached){a.origin=cached[0];a.dest=cached[1]}else if(!a.origin){a.origin=currentCode();a.dest='---'}if(a.alt==='ground'||a.alt==0){a.status='EN TIERRA';a.depKind='PROG';a.arrKind='PROG';a.depAt=now+18*60000;a.arrAt=a.depAt+75*60000}else{a.status='EN RUTA';a.depKind='REAL';a.arrKind='EST';a.depAt=now-32*60000;a.arrAt=now+28*60000}a.late=parseInt(a.hex||'0',16)%5===0;return a}
function norm(a){return {hex:a.hex,flight:(a.flight||a.r||a.hex||'').toString().trim(),r:a.r,t:a.t,lat:a.lat,lon:a.lon,alt:a.alt!=null?a.alt:a.alt_baro,gs:a.gs,track:a.track,dst:a.dst}}
function applyFilters(){AC=RAW.filter(a=>{if(!includeGround&&a.alt==='ground')return false;if(a.dst!=null&&a.dst*1.852>radiusKm)return false;if(!want[kindOf(a)])return false;if(!matchAirline(a,followAl))return false;if(followFlt&&!(a.flight||'').toUpperCase().replace(/\s+/g,'').includes(followFlt.toUpperCase().replace(/\s+/g,'')))return false;return a.lat!=null}).sort((a,b)=>(a.dst||99)-(b.dst||99)).slice(0,maxN);AC.forEach(enrich)}
async function pull(url){const res=await fetch(url);if(!res.ok)throw new Error('bad');return res.json()}
async function fillRoutes(list){const pending=list.filter(a=>a.flight&&!ROUTECACHE[(a.flight||'').replace(/\s+/g,'').toUpperCase()]).slice(0,12);await Promise.all(pending.map(async a=>{const cs=(a.flight||'').replace(/\s+/g,'').toUpperCase();try{const d=await pull('https://vrs-standing-data.adsb.lol/routes/'+cs.slice(0,2)+'/'+cs+'.json');const iata=(d._airport_codes_iata||'').split('-');if(iata.length>=2){ROUTECACHE[cs]=[iata[0],iata[iata.length-1]];a.origin=iata[0];a.dest=iata[iata.length-1]}}catch(e){}}))}
async function loadLive(){if(loading)return;loading=true;setLoad('Cargando '+currentCode()+(followAl?' \u00b7 '+followAl:'')+'...');const nm=Math.max(25,Math.round(radiusKm/1.852));const api='https://api.adsb.lol/v2/lat/'+center[0]+'/lon/'+center[1]+'/dist/'+nm;try{let data;try{data=await pull(api)}catch(e){try{data=await pull('https://api.allorigins.win/raw?url='+encodeURIComponent(api))}catch(e2){data=await pull('live.json?t='+Date.now())}}RAW=(data.ac||[]).map(norm);applyFilters();renderWall();fillRoutes(AC).then(()=>{applyFilters();renderWall()});const now=performance.now();AC.forEach(a=>{POS[a.hex]={lat:a.lat,lon:a.lon,t:now,gs:a.gs,track:a.track,alt:a.alt}});const st=document.getElementById('status');if(st)st.textContent=(followAl&&!AC.length)?'0 vuelos de '+followAl+' en '+currentCode():AC.length+' aeronaves \u00b7 '+currentCode()}finally{loading=false;hideLoad()}}
function displayPos(a){const p=POS[a.hex];if(!p||a.alt==='ground'||!p.gs||p.track==null)return {lat:a.lat,lon:a.lon};const dt=Math.min(25,(performance.now()-p.t)/1000);const km=p.gs*1.852*dt/3600;const rad=p.track*Math.PI/180;return {lat:p.lat+Math.cos(rad)*km/111,lon:p.lon+Math.sin(rad)*km/(111*Math.cos(p.lat*Math.PI/180))}}
function colors(){if(theme==='crt_green')return {fg:'#7dff7a',bg:'#031105'};if(theme==='atc_dark')return {fg:'#7ec8e3',bg:'#07090c'};if(theme==='navy')return {fg:'#8ab4ff',bg:'#061018'};if(theme==='red')return {fg:'#ff6b4a',bg:'#120606'};if(theme==='ice')return {fg:'#d9f6ff',bg:'#081016'};return {fg:'#e8b86d',bg:'#0a0805'}}
function applyThemeCss(){const {fg,bg}=colors();document.documentElement.style.setProperty('--list',fg);document.documentElement.style.setProperty('--bg',bg);document.documentElement.style.setProperty('--card',bg);const b=document.querySelector('.bezel');if(b)b.style.background=bg}
function grabForm(){view=document.getElementById('mode').value;theme=document.getElementById('theme').value;listStyle=document.getElementById('listStyle').value;applyThemeCss();const ap=findAirport(document.getElementById('preset').value);center=[ap[3],ap[4]];followAl=document.getElementById('followAl').value.trim();followFlt=document.getElementById('followFlt').value.trim();house=document.getElementById('house').checked;includeGround=document.getElementById('ground').value==='1';radiusKm=Number(document.getElementById('radius').value)||80;maxN=Math.max(4,Math.min(24,Number(document.getElementById('max').value)||16));rotateS=Math.max(3,Math.min(30,Number(document.getElementById('rotate').value)||7));want={air:fAir.checked,ga:fGa.checked,biz:fBiz.checked,heli:fHeli.checked};radar.style.display=view==='wall'?'none':'block';wall.style.display=view==='radar'?'none':'flex';wall.classList.toggle('full',view==='wall')}
document.getElementById('geo').addEventListener('click',()=>{navigator.geolocation.getCurrentPosition(pos=>{home=[pos.coords.latitude,pos.coords.longitude];document.getElementById('preset').value='CASA';center=home;document.getElementById('house').checked=true;house=true;setLoad('Cargando casa...');loadLive()})});
document.getElementById('demoHouse').addEventListener('click',()=>{document.getElementById('house').checked=true;document.getElementById('mode').value='radar';house=true;demoOver=true;if(!home)home=center.slice();grabForm();running=true;if(!looping)loop();updateOver()});
document.getElementById('f').addEventListener('submit',e=>{e.preventDefault();grabForm();page=0;setLoad('Cargando...');loadLive();running=true;if(view!=='wall'&&!looping)loop()});
['radius','max','ground','mode','theme','listStyle','rotate','followFlt','fAir','fGa','fBiz','fHeli','house'].forEach(id=>{document.getElementById(id).addEventListener('change',()=>{grabForm();if(id==='radius'||id==='followFlt'){setLoad('Cargando...');loadLive()}else{applyFilters();renderWall()}running=true;if(view!=='wall'&&!looping)loop()})});
function stClass(a){if(a.status==='EN TIERRA')return 'st-gnd';if(a.late)return 'st-late';return 'st-air'}
function logoHTML(a){const [iata,name,color]=airlineOf(a.flight);const fb='<div class="fb" style="background:'+color+'">'+(iata||name||'GA').slice(0,2)+'</div>';if(!iata)return fb;return '<img alt="'+iata+'" src="https://images.kiwi.com/airlines/64/'+iata+'.png" onerror="this.outerHTML=this.dataset.fb" data-fb=\''+fb+'\'>'}
function punct(a){return a.late?'DEMORADO':'A TIEMPO'}
function metLine(a){if(a.status==='EN TIERRA')return 'Preparandose \u00b7 '+punct(a);const m=a.arrAt?Math.round((a.arrAt-Date.now())/60000):null;return 'Alt '+fmtAlt(a.alt)+' \u00b7 '+ktToMph(a.gs)+(m!=null&&m>=0?' \u00b7 llega en '+m+' min':'')+' \u00b7 '+punct(a)}
function cardHTML(a){const [,name]=airlineOf(a.flight);let pct=a.status==='EN TIERRA'?6:Math.max(8,Math.min(94,((Date.now()-a.depAt)/(a.arrAt-a.depAt))*100||40));return '<div class="fa-card"><div class="fa-row"><div class="logo">'+logoHTML(a)+'</div><div><div class="fa-top"><span class="fa-id">'+(a.flight||a.hex)+'</span><span class="fa-st '+stClass(a)+'">'+(a.late?'DEMORA':a.status)+'</span></div><div class="fa-air">'+name+' \u00b7 '+(a.t||'-')+'</div></div></div><div class="fa-route"><span>'+(a.origin||'-')+'</span><span class="bar"><i style="width:'+pct+'%"></i></span><span>'+(a.dest||'-')+'</span></div><div class="fa-times"><div><span class="lbl">Despegue '+a.depKind+'</span><b>'+hhmm(a.depAt)+'</b></div><div><span class="lbl">Aterrizaje '+a.arrKind+'</span><b>'+hhmm(a.arrAt)+'</b></div></div><div class="fa-met">'+metLine(a)+'</div></div>'}
function clipCards(){if(listStyle==='carousel'||listStyle==='fids')return;const box=wall.getBoundingClientRect();[...wall.querySelectorAll('.fa-card')].forEach(c=>{const r=c.getBoundingClientRect();if(r.bottom>box.bottom+2||r.top<box.top-2)c.style.display='none'})}
function visibleList(){if(!AC.length)return [];if(listStyle==='carousel')return [AC[page%AC.length]];const h=wall.clientHeight||300;const n=listStyle==='fids'?Math.max(1,Math.floor((h-22)/30)):Math.max(1,Math.min(AC.length,12));const start=(page*n)%AC.length;const out=[];for(let i=0;i<Math.min(n,AC.length);i++)out.push(AC[(start+i)%AC.length]);return out}
function kmBetween(a,b){return Math.hypot((a[0]-b[0])*111,(a[1]-b[1])*111*Math.cos(a[0]*Math.PI/180))}
function overhead(){if(demoOver)return enrich({hex:'demo',flight:'AR1716',t:'A320',lat:center[0],lon:center[1],alt:4200,gs:210,track:180,origin:'AEP',dest:'COR'});if(!house||!home)return null;let best=null,bestD=9;AC.forEach(a=>{const d=kmBetween([displayPos(a).lat,displayPos(a).lon],home);if(d<bestD){bestD=d;best=a}});return best}
function updateOver(){const el=document.getElementById('overCard');if(!el)return;const a=overhead();if(!a||view==='wall'){el.style.display='none';return}el.style.display='block';el.innerHTML=cardHTML(a)}
function renderWall(){applyThemeCss();updateOver();const list=visibleList();if(listStyle==='fids')wall.innerHTML='<table class="fids"><thead><tr><th></th><th>VUELO</th><th>RUTA</th><th>DEP</th><th>ARR</th><th>ESTADO</th></tr></thead><tbody>'+list.map(a=>'<tr><td><div class="logo" style="width:24px;height:24px">'+logoHTML(a)+'</div></td><td>'+(a.flight||'')+'</td><td>'+(a.origin||'')+' → '+(a.dest||'')+'</td><td>'+hhmm(a.depAt)+'</td><td>'+hhmm(a.arrAt)+'</td><td>'+punct(a)+'</td></tr>').join('')+'</tbody></table>';else wall.innerHTML=listStyle==='carousel'?'<div class="carousel-wrap">'+(list[0]?cardHTML(list[0]):'')+'</div>':list.map(cardHTML).join('');requestAnimationFrame(clipCards)}
function angDiff(a,b){let d=a-b;while(d>Math.PI)d-=Math.PI*2;while(d<-Math.PI)d+=Math.PI*2;return d}
function loop(){if(!running||view==='wall'){looping=false;return}looping=true;const ctx=radar.getContext('2d');const w=radar.width=radar.clientWidth||640;const h=radar.height=radar.clientHeight||480;const {fg,bg}=colors();ctx.fillStyle=bg;ctx.fillRect(0,0,w,h);const left=view==='hybrid'?0.54:1;const cx=w*left*0.5,cy=h*0.52;const R=Math.min(cx,cy)*0.88;ctx.strokeStyle=fg+'22';[0.25,0.5,0.75,1].forEach(r=>{ctx.beginPath();ctx.arc(cx,cy,R*r,0,Math.PI*2);ctx.stroke()});ctx.beginPath();ctx.moveTo(cx,cy-R);ctx.lineTo(cx,cy+R);ctx.moveTo(cx-R,cy);ctx.lineTo(cx+R,cy);ctx.stroke();beam+=0.012;if(beam>Math.PI*3)beam-=Math.PI*2;if(((beam+Math.PI*2)%(Math.PI*2))<0.05&&performance.now()-lastSweep>8000){lastSweep=performance.now();loadLive()}if(performance.now()-lastRot>rotateS*1000){lastRot=performance.now();page++;renderWall()}ctx.strokeStyle=fg;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(cx+Math.cos(beam)*R,cy+Math.sin(beam)*R);ctx.stroke();ctx.fillStyle=fg;ctx.font='11px monospace';ctx.fillText(currentCode(),10,18);const span=Math.max(0.25,radiusKm/111);const labeled=new Set(visibleList().map(a=>a.hex));AC.forEach(a=>{const pos=displayPos(a);const x=cx+((pos.lon-center[1])/span)*R;const y=cy-((pos.lat-center[0])/span)*R;if(Math.abs(angDiff(Math.atan2(y-cy,x-cx),beam))<0.08)a.paint=1;a.paint=Math.max(0.28,(a.paint||0.28)-0.0025);ctx.globalAlpha=a.paint;ctx.fillStyle=fg;ctx.save();ctx.translate(x,y);ctx.rotate((a.track||0)*Math.PI/180);ctx.beginPath();ctx.moveTo(0,-6);ctx.lineTo(3.6,5);ctx.lineTo(-3.6,5);ctx.closePath();ctx.fill();ctx.restore();if(labeled.has(a.hex))ctx.fillText((a.flight||'').slice(0,8),x+8,y-8);ctx.globalAlpha=1});if(demoOver){ctx.strokeStyle='#fff';ctx.beginPath();ctx.arc(cx,cy,18,0,Math.PI*2);ctx.stroke();ctx.fillStyle='#fff';ctx.beginPath();ctx.moveTo(cx,cy-10);ctx.lineTo(cx+7,cy+8);ctx.lineTo(cx-7,cy+8);ctx.closePath();ctx.fill();ctx.fillText('SOBRE CASA',cx+14,cy-8);updateOver()}requestAnimationFrame(loop)}
grabForm();setLoad('Cargando AEP...');loadLive().then(()=>{running=true;if(view!=='wall')loop()});
setInterval(()=>{if(view!=='wall')return;if(performance.now()-lastRot>rotateS*1000){lastRot=performance.now();page++;renderWall()}},400);
window.addEventListener('resize',()=>renderWall());
